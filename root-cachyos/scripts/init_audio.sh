#!/bin/bash
# Placeholder — pas de config PipeWire spéciale nécessaire pour l'instant,
# PulseAudio/PipeWire par défaut suffisent pour Sunshine (capture via
# pipewire-pulse) et Steam. À enrichir si latence/sink par défaut posent
# problème une fois testé.
exit 0
