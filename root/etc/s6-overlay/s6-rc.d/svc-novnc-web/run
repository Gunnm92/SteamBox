#!/usr/bin/with-contenv bash

# Sert les fichiers statiques noVNC — serveur dédié minimal plutôt que de
# toucher à la config nginx existante (générée pour Selkies, sensible aux
# modifications).
exec s6-setuidgid abc \
  python3 -m http.server 6080 --directory /usr/share/novnc --bind 0.0.0.0
